services:
  myownhub:
    build: .
    image: myownhub:latest
    container_name: myownhub
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/public/uploads
    environment:
      - PORT=3000
    restart: unless-stopped
