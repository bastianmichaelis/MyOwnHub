services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - hub-data:/app/data
    environment:
      - SESSION_SECRET=ersetze-mich-durch-ein-sicheres-passwort
      - PORT=3000
    restart: unless-stopped

volumes:
  hub-data:
